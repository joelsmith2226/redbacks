class LeaderboardListEntry {
  String name;
  String teamName;
  int gwPts;
  int totalPts;
  String uid;
  int preAppPoints;

  LeaderboardListEntry(this.name, this.teamName, this.gwPts, this.totalPts, this.uid, this.preAppPoints);
}